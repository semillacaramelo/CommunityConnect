"Como experto en Python y trading algorítmico, necesito que finalices y optimices el proyecto de nuestro bot de trading para Deriv.com para que opere en entornos live (cuentas demo y real) y esté completamente listo para ejecutarse en VS Code. He actualizado la documentación (readme.md y documentation.md) para enfocarse exclusivamente en la ejecución en entornos live, pero es necesario realizar las siguientes acciones:

1. **Verificación y Corrección de la Implementación:**
   - Revisa y corrige posibles errores en los módulos de conexión a la API (por ejemplo, en `deriv_connector.py`), garantizando la correcta conexión a entornos live.
   - Verifica la gestión de tokens API y la transición entre el modo demo y real (por ejemplo, en `config.py`), asegurándote de que el sistema de autenticación funcione correctamente en ambos entornos.
   - Realiza pruebas de conectividad y autenticación en la cuenta demo y la cuenta real para confirmar la estabilidad del sistema.

2. **Limpieza y Optimización del Proyecto:**
   - Elimina todos los archivos y componentes que no sean esenciales para la operación en entornos live (por ejemplo, archivos de pruebas, mockups, datos de entrenamiento antiguos, etc.).
   - Conserva únicamente los módulos y recursos necesarios para el funcionamiento del bot, manteniendo la estructura modular limpia y organizada.
   - Asegúrate de que la estructura del proyecto sea clara y esté preparada para futuras expansiones, sin archivos innecesarios.

3. **Configuración de Dependencias y Variables de Entorno:**
   - Verifica que todas las dependencias estén correctamente instaladas y configuradas para su ejecución en VS Code.
   - Configura adecuadamente las variables de entorno requeridas (como `DERIV_API_TOKEN_DEMO`, `DERIV_API_TOKEN_REAL`, etc.) para que el proyecto se ejecute sin problemas en un entorno local.
   - Actualiza la documentación para incluir instrucciones claras sobre cómo configurar y ejecutar el proyecto en VS Code.

El objetivo final es tener un bot de trading completamente optimizado y funcional, sin archivos innecesarios, y listo para operar en entornos live (tanto en cuenta demo como en cuenta real). Por favor, implementa estas mejoras manteniendo toda la información relevante en la documentación actual y añadiendo las instrucciones necesarias para la correcta configuración y ejecución en VS Code."